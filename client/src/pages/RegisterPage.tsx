import React from "react";
import Register from "../components/forms/auth/Auth";

const RegisterPage = () => {
  return (
    <section>
      <Register />
    </section>
  );
};

export default RegisterPage;
