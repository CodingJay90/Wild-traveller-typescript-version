import React from "react";
import Profile from "../components/dashboard/Profile";

const Dashboard = () => {
  return (
    <section>
      <Profile />
    </section>
  );
};

export default Dashboard;
